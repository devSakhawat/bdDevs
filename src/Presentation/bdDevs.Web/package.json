{
  "name": "bdDevs-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "build:prod": "NODE_ENV=production node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@microsoft/signalr": "^8.0.0"
  }
}
